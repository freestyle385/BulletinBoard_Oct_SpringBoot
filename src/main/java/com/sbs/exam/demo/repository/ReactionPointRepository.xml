<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.sbs.exam.demo.repository.ReactionPointRepository">

	<insert id="addIncreasingGoodRpInfo">
		INSERT INTO reactionPoint
		SET regDate = NOW(),
		updateDate =
		NOW(),
		boardId = #{boardId},
		articleId = #{articleId},
		memberId = #{memberId},
		pointTypeCode = 1
	</insert>
	
	<insert id="addDecreasingGoodRpInfo">
		INSERT INTO reactionPoint
		SET regDate = NOW(),
		updateDate =
		NOW(),
		boardId = #{boardId},
		articleId = #{articleId},
		memberId = #{memberId},
		pointTypeCode = -1
	</insert>

	<insert id="addIncreasingBadRpInfo">
		INSERT INTO reactionPoint
		SET regDate = NOW(),
		updateDate =
		NOW(),
		boardId = #{boardId},
		articleId = #{articleId},
		memberId = #{memberId},
		pointTypeCode = 2
	</insert>
	
	<insert id="addDecreasingBadRpInfo">
		INSERT INTO reactionPoint
		SET regDate = NOW(),
		updateDate =
		NOW(),
		boardId = #{boardId},
		articleId = #{articleId},
		memberId = #{memberId},
		pointTypeCode = -2
	</insert>

	<select id="getRpInfoByMemberId" resultType="Integer">
		SELECT SUM(pointTypeCode) 
		FROM reactionPoint 
		WHERE articleId = #{articleId} AND memberId = #{memberId};
	</select>
</mapper> 